//
//  RenderTarget.h
//  Oculon
//
//  Created by Ehsan Rezaie on 2014-12-12.
//
//

#pragma once

#include "Scene.h"
#include "cinder/gl/Fbo.h"

namespace oculon
{
    /*
typedef std::shared_ptr<class RenderTarget> RenderTargetRef;

    

class RenderTarget
{
public:
    RenderTarget( int32_t width, int32_t height );
    
    void addScene( SceneRef scene );
    
    
    virtual void renderScenes();
    
    ci::Area       getBounds()      { return mFbo.getBounds(); }
    ci::Vec2i      getSize()        { return mFbo.getSize(); }
    
protected:
    virtual void setupFbo( int32_t width, int32_t height );
    void clearFbo();
    virtual void preRender();
    virtual void postRender();
    
protected:
    SceneList       mScenes;
    ci::gl::Fbo     mFbo;
};
*/
}
